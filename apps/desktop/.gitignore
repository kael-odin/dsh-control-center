# Electron self-host smoke creates an isolated DSH home here.
.smoke-home/
# electron-builder output (unpacked app, installers).
release/
# Bundled Node.js runtime (downloaded, not committed).
vendor/node/
